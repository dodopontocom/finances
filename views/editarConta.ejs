<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Editar Conta</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <h1>✏️ Editar Conta</h1>
  <form action="/contas/<%= conta._id %>/editar" method="POST" class="formulario">
    <input type="text" name="nome" value="<%= conta.nome %>" required />
    <input type="text" name="vencimento" value="<%= conta.vencimento %>" required />
    <input type="number" step="0.01" name="valor" value="<%= conta.valor %>" required />
    <select name="status">
      <option value="pendente" <%= conta.status === 'pendente' ? 'selected' : '' %>>Pendente</option>
      <option value="pago" <%= conta.status === 'pago' ? 'selected' : '' %>>Pago</option>
      <option value="atrasado" <%= conta.status === 'atrasado' ? 'selected' : '' %>>Atrasado</option>
    </select>
    <button type="submit">Atualizar</button>
  </form>
  <a href="/contas">← Voltar</a>
</body>
</html>

